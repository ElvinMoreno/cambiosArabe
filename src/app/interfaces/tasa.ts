export interface Tasa {
  tasaVenta?: number; // El signo de interrogación indica que este campo es opcional
  tasaCompra?: number; // El signo de interrogación indica que este campo es opcional
  fechaTasa?: Date; // El signo de interrogación indica que este campo es opcional
}
