export interface Tasa {
  id?: number; // El signo de interrogación indica que este campo es opcional
  tasaVenta?: number; // El signo de interrogación indica que este campo es opcional
  bolivares?: number; // El signo de interrogación indica que este campo es opcional
  pesos?: number; // El signo de interrogación indica que este campo es opcional
  sumaTasa?: number; // El signo de interrogación indica que este campo es opcional
}
